public class ReturnCodes {
    public enum Codex {
        EMPTY_NICK_OR_PASS,
        ALREADY_REGISTERED,
        USER_NOT_FOUND,
        WRONG_PASSWORD,
        ALREADY_LOGGED_IN,
        ALREADY_LOGGED_OUT,
        ALREADY_FRIENDS,
        NOT_A_FRIEND,
        USER_NOT_ONLINE,
        SUCCESS,
        COMMAND_NOT_FOUND
    }
}
